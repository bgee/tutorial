cmake_minimum_required (VERSION 2.6)
project (Tutorial)

# Include the subfolder and the Eigen3 folder
include_directories()

# Add the code file and the executable's name (this corresponds to -o in g++, try it!)
add_executable()
